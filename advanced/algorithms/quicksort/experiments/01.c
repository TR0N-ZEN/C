#include <stdio.h>

int main() {

  // does the integer division (written as a single forward slash)
  // floor the real result?
  printf("%d\n", 33/2);
  // prints 16
  // so it floors the real result

}
